# Modules
Every module is its own CMake project, and built as an elf relocatable object file.  

## Template
There is a simple Template project in ./Template that can be copied and modified to create new modules.
