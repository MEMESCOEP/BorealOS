# Binaries
These are the userspace binaries for BorealOS. No longer part of the kernel itself.

## Library and executables
Library files are in ./lib, and executable files are in ./bin.

## Template
There is a simple Template project in ./Template that can be copied and modified to create new binaries.